apiVersion: v1
kind: Service
metadata:
  name: quakewatch-service
  namespace: quakewatch
spec:
  type: NodePort
  selector:
    app: quakewatch
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
